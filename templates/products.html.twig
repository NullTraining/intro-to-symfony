{% extends 'base.html.twig' %}



{% block body %}

    <table>
    {% for product in products %}
        <tr>
            <td>
                {{ product.id }}
            </td>
            <td>
                {{ product.title }}
            </td>
        </tr>
    {% endfor %}
    </table>
{% endblock %}
